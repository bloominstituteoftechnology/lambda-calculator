import React from "react";

const OperatorButton = (props) => {
  return (
    <>
      <button className="oprateButtonz">{props.OperateButton.value}</button>
    </>
  );
};

export default OperatorButton;